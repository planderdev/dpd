# DPD Upload Manifesto

이 문서는 DPD 웹사이트를 GitHub 또는 정적 호스팅/외부 CMS에 업로드할 때 함께 확인할 운영 가이드입니다.

## 업로드 원칙

- 프론트 사이트 디자인과 관리자 입력 데이터 구조는 1:1 연결을 유지합니다.
- 프론트 CSS와 관리자 CSS는 서로 개입하지 않도록 분리합니다.
- 아이콘은 Remix Icon만 사용합니다.
- 사이트 기본 폰트는 SUIT를 사용합니다.
- 주요 액센트 컬러는 `#E01261` 기준으로 유지합니다.
- 캐시, 스크린샷, 임시 파일, 로컬 로그는 업로드하지 않습니다.

## 업로드 대상

아래 항목은 배포에 필요한 파일입니다.

- `index.html`
- `kr/`
- `en/`
- `admin/`
- `assets/`
- `file/`
- `favicon.ico`
- `README.md`
- `MANIFESTO.md`

## 업로드 제외 대상

아래 항목은 저장소 또는 배포 대상에 포함하지 않습니다.

- `node_modules/`
- `.cache/`
- `.tmp/`
- `tmp/`
- `temp/`
- `screenshots/`
- `captures/`
- OS 임시 파일
- 로컬 환경 변수 파일

## 관리자 사용 가이드

관리자는 `admin/login.html`에서 접근합니다.

- ID: `admin`
- Password: `dpd2026`

현재 관리자 DB는 브라우저 저장소 기반입니다. 백엔드 연결 전까지는 같은 브라우저 안에서만 수정 데이터가 유지됩니다.

## 백엔드 연결 시 작업 기준

1. `assets/js/cms-api-adapter.js`의 엔드포인트를 채우고, 관리자/프론트 페이지에서 `cms-store.js` 다음에 불러옵니다. 저장 위치에 관한 코드는 `cms-store.js`의 저장 어댑터 한 곳에만 있으므로 화면 코드는 고치지 않습니다.
2. 파일 첨부 UI는 유지하되 실제 업로드 경로는 서버 응답값을 사용합니다.
3. 관리자 입력 필드와 프론트 출력 데이터 키가 바뀌지 않도록 API 응답 구조를 맞춥니다.
4. 자료실 파일 확장자 표기는 첨부 파일명에서 자동 추출하는 현재 규칙을 유지합니다.
5. 공급품목의 제조 국가 라벨은 관리자 옵션과 프론트 필터가 같은 값을 쓰도록 유지합니다.
6. `DpdCmsStore.getData()`는 동기 호출입니다. API 어댑터는 `hydrate()`에서 데이터를 받아 메모리에 들고 있다가 돌려주는 방식으로 맞춥니다.

## 다국어 유지 기준

- 국문 페이지 하나로 국·영문을 모두 보여줍니다. 영문 전용 HTML을 새로 만들지 않습니다.
- 새 문구를 추가하면 `assets/js/content-data.js`의 `manual.text`(본문) 또는 `manual.attrs`(title/aria-label/alt/placeholder)에 번역을 같이 넣습니다.
- 품목명이 끼어드는 접근성 문구는 개별 항목이 아니라 `manual.attrPatterns`의 패턴으로 처리합니다.
- 헤더 메뉴 문구를 바꾸면 모든 페이지의 GNB와 `manual.text`를 함께 갱신합니다.

## 업로드 후 확인 체크리스트

- 메인 히어로 비디오가 재생되는지 확인
- 메인 개발품목/공급품목 슬라이드가 동작하는지 확인
- 공급품목 라이트박스가 열리는지 확인
- 공지사항/자료실 카드 레이아웃이 유지되는지 확인
- 문의 폼 파일첨부 UI가 깨지지 않는지 확인
- 관리자 로그인, 목록, 추가, 수정, 삭제, 토스트 메시지가 동작하는지 확인
- 모바일 헤더와 햄버거 메뉴가 정상 동작하는지 확인
- `?lang=en`으로 주요 페이지를 열어 한글이 남아 있지 않은지 확인
- `scripts/` 점검 스크립트 네 개가 모두 통과하는지 확인
